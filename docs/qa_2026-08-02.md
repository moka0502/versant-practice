# QAメモ（2026-08-02、実機テストで発覚した品質問題への対応）

ユーザーの実機テストで複数のバグが見つかり、場当たり的な個別修正ではなく体系的な横展開チェック・意地悪テストを実施することにした。このファイルは作業ログ。最終的な結果はユーザーへの報告と`docs/backlog.md`への反映で締める。

## ①これまでに見つかったバグ・改善点一覧

| # | 内容 | 発見経緯 | 状態 |
|---|---|---|---|
| 1 | TOP画面の「練習する」「統計を見る」ボタンが隙間なく接触 | ユーザー実機 | 修正済み（`.btn-large`にmargin-bottom追加） |
| 2 | 正誤判定後の演出（reaction-overlay）中、下の判定ボタンがクリック可能なままで二重判定される | ユーザー実機（「18問中16問」のズレとして発覚） | 修正済み（演出中はボタンをdisabled化） |
| 3 | ダークモードで速度チップ（0.75x等）の非アクティブ時の文字が背景とほぼ同化して見えない | ユーザー実機 | 修正済み（ハードコード色→テーマ変数） |
| 4 | 誤答理由チップが「選ばなくてもよい」と伝わりにくい | ユーザー実機 | 修正済み（ラベル文言変更） |
| 5 | 初級セット1だけ配信問題が15問（他は16問） | ユーザー実機 | 修正済み（原因はrep-0013が検品でverified=False、ダミー1問追加で16問に） |
| 6 | 読み込み中インジケーター表示中でも「はじめる」ボタンが押せる | ユーザー実機 | 未再現・コード上は無効化ロジックあり。要継続観察 |
| 7 | 「16問終わってもサマリー画面に行かない」 | ユーザー実機 | #2と同一原因の可能性が高いが未再検証 |
| 8 | 演出中はscreen-answer上の判定ボタン以外（マーク・録音・フレーズ登録・戻る）も同じ理由でクリックが素通りしていた | 横展開チェックで発見 | 修正済み（画面全体をpointer-events:noneに） |
| 9 | フレーズ登録ボタンを連打すると同じフレーズが重複登録される | 横展開チェックで発見 | 修正済み（`savePhrase()`に重複チェック追加） |
| 10 | 録音ボタンを初回マイク許可待ち中に連打すると録音状態が壊れうる | 横展開チェックで発見 | 修正済み（busyフラグガード追加） |

## ⑤ビジネスロジック・データシナリオのテスト（完了）

指摘を受け、「UIが壊れないか」だけでなく「機能のロジックが正しい結果を出すか」を実データシナリオで検証。localStorageを直接操作して境界値・複数パターンを再現（実プレイでは時間がかかりすぎるため）。全項目パス、バグなし。

| 項目 | シナリオ | 結果 |
|---|---|---|
| Leech判定の境界値 | attempts=2(不足)/正答率ちょうど50%/49%/33% | 閾値通りに正しく判定 |
| 苦手リストの並び順 | 7問中attempts不足1問を除外、正答率昇順ソート、上限5件 | 全て正しい |
| コイン経済 | 残高不足時の購入拒否／成功時の消費／フリーズ上限2個到達時の拒否 | 全て正しい |
| SRS間隔計算(簡易SM-2) | perfect連続でintervalDaysが1→6→16と増加、noneでrepetitions/intervalDaysがリセット | 全て正しい |
| 誤答理由のバリエーション | 理由未選択のまま／理由選択済み／同一問題を2回間違えてそれぞれ別理由を選択 | ログが正しく分離・更新される、誤答レビュー画面の表示も正しい |
| レベル別内訳 | 初級100%・中級50%・上級0%のデータを混在させて集計 | 各レベルが独立して正しく集計される |
| 週次トレンド | 今週の対象期間だけデータあり／先週分もあり／先週分なし | trend計算・null分岐とも正しい |

## ⑥WebKitエンジン・複数画面サイズでの再テスト（完了）

これまでChromiumのみ・390×844の1パターンのみだった検証を、WebKit（Safariに近いレンダリングエンジン。実機iOS Safariとは別物だが、Chromiumより近い）× 4画面サイズ（iPhone SE 375×667／標準390×844／Pro Max 430×932／横向き844×390）に拡張。

**重大バグを1件発見・修正**: オンボーディング画面（初回起動時の説明オーバーレイ）に`overflow`指定が無く、画面の高さが低い条件（横向き等）でカード下部の「はじめる」ボタンが完全に画面外へはみ出し、**スクロールもできず物理的に押せない＝先に進めなくなる**状態だった。オンボーディングの質問追加（レベル感・目的の2問、2026-08-02実装）でカードが縦に伸びたことも一因。

修正: `.onboarding-overlay`に`overflow-y: auto`を追加し、`.onboarding-card`を`align-items:center`ではなく`margin:auto 0`方式に変更（内容が収まる時は中央寄せ、はみ出た時は上端からスクロールで全体に到達できる）。同じCSSパターン（`position:fixed`+`align-items:center`でoverflow未対応）を使っていた`.reaction-overlay`にも念のため同じ対策を横展開。

修正後、WebKit×4画面サイズ全パターンで再検証し、オンボーディング完走・二重タップガード・consoleエラーなしを確認。

**その他の発見（バグではなかったもの）**: 横向き×WebKit環境で録音ボタン一式が非表示だったが、これはWebKitのヘッドレス環境がマイク機能非対応だったための正しいフォールバック動作（`recordingSupported()`がfalseを返し、設計通り機能自体を隠していた）。

## ④総括・要判断事項

- #6・#7は自動テストでは再現できなかった。#7は#2（判定ボタンの二重発火）と同じ症状（進捗のズレ）のため、#2の修正で解決済みの可能性が高い。ユーザーに実機で再度確認してもらう
- ダークモード配色・意地悪テスト（連打/中断復帰/高速遷移/設定切替）は横展開チェックの結果、新規バグなし
- 今回の教訓: 個別バグを直すだけでなく「同じ原因パターンが他にもないか」を都度確認する。特に「演出・アニメーション中でも下の要素がクリック可能」という構造的な穴は、今後同種の演出を追加する際に再発しやすいので注意

## ②横展開チェック: 二重発火系（完了）

`app.js`の全`addEventListener("click", ...)`（約35箇所）を1つずつ確認。

**根本原因の再整理**: `judge()`のバグは「非同期処理だから」ではなく、「reaction-overlayが`pointer-events:none`のため、演出中も下のscreen-answer上の全ボタンにクリックが素通りする」という構造的な問題だった。判定ボタンだけでなく、mark-button・save-phrase-button・record-button・戻るボタン等、screen-answer上の**全ボタンが同じリスクを持っていた**。

**対応**: 個別のボタン無効化ではなく、`judge()`実行中は`screen-answer`全体に`pointer-events:none`を掛ける方式に変更（`setAnswerScreenBusy()`）。これで演出中のscreen-answer上の操作は種類を問わず一括でブロックされる。

**追加で見つけて直した2件**:
- `save-phrase-button`: 連打すると同じフレーズが重複登録される → `savePhrase()`に重複チェックを追加
- `record-button`: 初回のマイク許可待ち中に連打すると`startRecording()`が二重に走りMediaRecorderの状態が壊れうる → ボタンに`busy`フラグガードを追加

**他は問題なしと判断したもの**: ナビゲーション系・トグル系（テーマ切替、マーク、フィルタ等）はいずれも冪等（連打しても同じ状態に収束する）か、二重発火してもトースト表示が一瞬重複する程度でデータ破損が起きないことを確認。

## ②横展開チェック: ダークモード配色（完了）

`style.css`の`color:`/`background:`のハードコード値（`var(--`以外）を全件棚卸しし、それぞれが「自己完結した色ペア（背景も文字色も固定値で、テーマに関係なく成立する意図的な配色）」か「テーマ変数の背景の上にハードコード文字色が乗っていて破綻しうるもの」かを分類。後者に該当したのはspeed-chip（修正済み）のみだった。

その後、実際に全画面をPlaywrightでダークモードスクリーンショット（TOP/Part選択/レベル選択/リスニング/回答/正誤演出/統計(アコーディオン全開)/誤答レビュー）して目視確認。見た目上「文字が薄い」と感じた箇所（Part選択のロック済みカード、フレーズ登録ボタン、復習セットボタン）が3つあったが、いずれも`getComputedStyle`で実測した結果、意図した色が正しく適用されており誤検知だった（縮小スクリーンショットでの見誤り、または絵文字フォントの描画差によるもの）。**新規のダークモード不具合は追加で見つからなかった。**

## ③意地悪テスト（完了）

Playwrightで4種類の「意地悪ユーザー」を再現し、自動チェックを実施。全項目パス、新規バグなし。

| ペルソナ | やったこと | 結果 |
|---|---|---|
| 連打魔 | mark-button4連打、judge-none5連打（間隔0.05秒） | マーク数・totalCount・XPとも1回分のみ加算（正常） |
| 中断・復帰魔 | セッション途中でリロード→再開バナー→再開 | session-snapshot保存・再開後のcurrentIndexとも正常 |
| 高速画面遷移魔 | Part選択⇄レベル選択を8往復 | consoleエラーなし |
| 設定連打魔 | レベルチップを5回連続で切り替え | 見た目(active表示)とstateが一致 |

（`start-button`は1クリックで即座に画面遷移するため、そもそも物理的に連打できない構造だと確認できた＝安全）
