{
	"name": "versant-practice",
	"image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye",

	// このフォルダを開いて「Reopen in Container」すると、Windows側のこのフォルダが
	// そのまま /workspaces/versant-practice にバインドマウントされる(既定の挙動)。
	// コンテナを消してもファイル実体はWindows側に残る。

	// Obsidian vaultのdaily-notesフォルダを追加でバインドマウント(SessionEndフックの書き込み先)
	"mounts": [
		"source=${localEnv:USERPROFILE}/OneDrive/デスクトップ/202607_Obsidian/ObsidianVault/daily-notes,target=/mnt/obsidian-daily-notes,type=bind,consistency=cached"
	],

	"postCreateCommand": "mkdir -p ~/.claude/scripts && cp .devcontainer/global-standards.md ~/.claude/CLAUDE.md && cp .devcontainer/vault_archive.py ~/.claude/scripts/vault_archive.py && cp .devcontainer/global-settings.json ~/.claude/settings.json && pip install -r requirements.txt -r requirements-dev.txt",

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"charliermarsh.ruff"
			]
		}
	}
}
